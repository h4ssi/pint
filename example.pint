
(def hello (fn () (print 1)))

(hello)

(def double (fn (x) (plus x x)))

(print (double (plus 1 1)))

(def rec (fn (limit rr)
           (if (eq limit 0)
             0
             (plus limit (rr (plus limit (minus 1)))))))

(print (rec 5 rec))

(print (fn () 1))

(print (quote a) (quote 1337) (quote (a 1 c)))
